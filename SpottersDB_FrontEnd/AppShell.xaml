<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="SpottersDB_FrontEnd.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:SpottersDB_FrontEnd"
    xmlns:views="clr-namespace:SpottersDB_FrontEnd.Classes.Views"
    Shell.FlyoutBehavior="Flyout"
    Title="SpottersDB_FrontEnd">

    <FlyoutItem Title="Home">
        <Tab Title="Countries">
            <ShellContent
                Title="Countries"
                ContentTemplate="{DataTemplate views:HomeCountries}"></ShellContent>
        </Tab>
        <Tab Title="Countries">
            <ShellContent
                Title="Countries"
                ContentTemplate="{DataTemplate views:HomeCountries}"></ShellContent>
        </Tab>
    </FlyoutItem>
    <!--<FlyoutItem Title="Home">
        <ShellContent 
            Title="Edit Page"
            ContentTemplate="{DataTemplate views:EditPage}"
            />
    </FlyoutItem>-->

    <FlyoutItem Title="Edit">
        <ShellContent 
            Title="Edit Page"
            ContentTemplate="{DataTemplate views:EditPage}"
            />
    </FlyoutItem>

</Shell>
